# 🚀 MediaMeter - Быстрый Старт

## 📦 Что я подготовил для тебя:

✅ `requirements.txt` - все зависимости  
✅ `Dockerfile` - для Railway  
✅ `railway.toml` - конфиг Railway  
✅ `.env.example` - переменные окружения  
✅ `collectors_utils_v2.py` - исправленные утилиты  
✅ `rss_parser_v3.py` - RSS коллектор для Railway  
✅ `RAILWAY_DEPLOY.md` - подробная инструкция  

---

## 🎯 ПЕРВЫЙ ЗАПУСК: RSS на Railway (30 минут)

### Шаг 1: GitHub

```bash
# Создай репозиторий на GitHub (mediameter)
# Склонируй его локально

cd mediameter

# Скопируй все файлы:
# - весь backend/ 
# - весь frontend/
# - requirements.txt
# - Dockerfile
# - railway.toml
# - .env.example
# - collectors/ папку с коллекторами

git add .
git commit -m "Initial setup"
git push origin main
```

### Шаг 2: Railway

1. Зайди на https://railway.app
2. Sign up с GitHub
3. "Start a New Project" → "Deploy from GitHub repo"
4. Выбери `mediameter`
5. Railway автоматически создаст PostgreSQL! 🎉

Жди 2-3 минуты пока развернется...

### Шаг 3: Проверь что работает

Railway даст тебе URL, например: `https://mediameter-production.up.railway.app`

Открой в браузере:
```
https://mediameter-production.up.railway.app/docs
```

Если загрузилась Swagger документация - **ВСЁ РАБОТАЕТ!** ✅

### Шаг 4: Добавь персон в БД

В Swagger:
1. Разверни `POST /v1/persons`
2. Нажми "Try it out"
3. Вставь:

```json
{
  "name": "Володимир Зеленський",
  "slug": "zelenskyy",
  "name_variants": ["Zelenskyy", "Zelensky"],
  "topics": ["politics"]
}
```

4. Нажми "Execute"

Добавь ещё 2-3 персон!

### Шаг 5: Запусти RSS Collector

#### Вариант A: На Railway (рекомендую)

1. В Railway → Project Settings → New Service
2. Выбери "GitHub"
3. Выбери свой репозиторий снова
4. В Build settings установи:

```
Command: python collectors/rss_parser_v3.py
```

5. Нажми Deploy
6. Смотри логи - должны быть новости про твоих персон!

#### Вариант B: Локально (для тестирования)

```bash
# Создай .env файл (скопируй из .env.example)
cp .env.example .env

# Обнови в .env:
# API_BASE_URL=https://твой-railway-url.railway.app

# Запусти коллектор
python collectors/rss_parser_v3.py
```

---

## 📊 Что происходит:

1. ✅ Backend на Railway собирает запросы
2. ✅ RSS Collector каждый час проверяет новости
3. ✅ Когда найдёт новость про твоих персон → отправит на Backend
4. ✅ Backend сохраняет в PostgreSQL БД

---

## 📱 ВТОРОЙ ЭТАП: Telegram (когда RSS работает)

Когда убедишься что RSS работает:

1. Получи `API_ID` и `API_HASH` от https://my.telegram.org
2. Добавь в Railway → Variables:
   ```
   TELEGRAM_API_ID=your_api_id
   TELEGRAM_API_HASH=your_api_hash
   ```
3. Создай новый сервис в Railway:
   ```
   Command: python collectors/telegram_bot_v2.py
   ```

---

## 🐦 ТРЕТИЙ ЭТАП: Twitter (когда Telegram работает)

1. Получи Bearer Token от https://developer.twitter.com/en/portal/dashboard
2. Добавь в Railway → Variables:
   ```
   TWITTER_BEARER_TOKEN=your_bearer_token
   ```
3. Создай новый сервис в Railway:
   ```
   Command: python collectors/twitter_monitor_v2.py
   ```

---

## 🌐 FRONTEND: Streamlit Dashboard

### Локально (для разработки):

```bash
streamlit run frontend/app.py
```

Открой http://localhost:8501

### На Railway:

1. Новый сервис в Railway
2. Command:
   ```
   streamlit run frontend/app.py --server.port=$PORT --server.address=0.0.0.0
   ```

**ВАЖНО:** В `frontend/app.py` обнови:
```python
API_BASE_URL = "https://твой-backend-url.railway.app"
```

---

## 📋 ЧЕК-ЛИСТ:

- [ ] GitHub репозиторий создан
- [ ] Файлы запушены в GitHub
- [ ] Backend развёрнут на Railway
- [ ] Backend работает (есть /docs)
- [ ] Персоны добавлены в БД
- [ ] RSS Collector запущен
- [ ] Collector собирает новости (видны в логах)
- [ ] Frontend работает локально
- [ ] Дашборд показывает данные

---

## 🆘 ПОМОЩЬ:

### Backend не работает?
- Смотри логи в Railway → Logs
- Обычно ошибка с DATABASE_URL
- Railway автоматически устанавливает правильный URL

### RSS не собирает данные?
- Проверь что есть персоны:
  ```
  GET https://твой-url/v1/persons
  ```
- Смотри логи Collector
- Проверь что API_BASE_URL правильный

### Frontend не работает?
- Обнови API_BASE_URL на реальный URL Backend на Railway
- Перезагрузи Streamlit

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ (после RSS):

1. Добавить Telegram коллектор
2. Добавить Twitter коллектор
3. Интегрировать ChatGPT для анализа
4. Улучшить UI дашборда
5. Добавить фильтры по датам, источникам и т.д.

---

## 💬 ВОПРОСЫ?

Все ошибки и проблемы обычно видны в логах Railway или в консоли.

Дай мне скриншот ошибки и я помогу! 🙌

---

**НАЧНИ С ШАГа 1 - GitHub! Потом скажи мне когда Backend загрузится на Railway!** 🚀
